#include <iostream>

using namespace std;

//if same +1, diff -3, adding D: first -3, from second -1
char str1[] = "aabbcc";
char str2[] = "bbcaab";

int main() {
  int ret = findMin();
  return 0;
}
